name: Client CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  native:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: Install native dependencies
        run: |
          sudo apt-get update
          sudo apt-get install --yes build-essential cmake ninja-build libgtk-3-dev libsdl2-dev libopenal-dev
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - run: ./build.sh
      - run: dotnet format --verify-no-changes --no-restore
      - run: dotnet build LibreLancer.sln --configuration Release --no-restore --warnaserror
      - run: dotnet test src/LibreLancer.Tests/LibreLancer.Tests.csproj --configuration Release --no-build --logger trx --collect:"XPlat Code Coverage"
